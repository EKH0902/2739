# Plan & Work Log

이 문서는 `2739` 저장소에서 진행한 작업 내역, 세부 수정 사항, 그리고 앞으로의 계획을 기록하는 로그입니다.

## 작업 로그

### 2026-07-16
- 저장소 초기화 (빈 저장소였음): `main` 브랜치 생성
- 스타터 소스 파일 추가 (PR #1, merged):
  - `main.c` — hello world 출력 엔트리포인트
  - `effect.c` — `effect()` 스텁 함수
  - `effect2.c` — `effect2()` 스텁 함수
  - `reboot.c` — `reboot()` 스텁 함수
  - `start.c` — `start()` 스텁 함수
  - `start.py` — 실행 가능한 `start()` 엔트리포인트
  - `license.md` — MIT 라이선스
- `plan.md` 추가 (본 문서): 작업 로그 및 계획 기록 시작

## 계획 (TODO)
- (여기에 앞으로 할 작업을 추가)

## 메모
- 이 로그는 작업/수정/계획을 계속 누적해서 기록합니다.

---

# 계획 및 작업 로그 (한국어 제목판)

이 문서는 `2739` 저장소의 작업 내역·세부 수정·계획을 기록하는 로그입니다.

## 작업 로그
### 2026-07-16
- 저장소 초기화(빈 저장소): `main` 브랜치 생성
- 스타터 소스 파일 추가 (PR #1, 병합됨): `main.c`, `effect.c`, `effect2.c`, `reboot.c`, `start.c`, `start.py`, `license.md`
- `plan.md` 추가: 작업 로그 및 계획 기록 시작

## 계획 (할 일)
- (앞으로 할 작업을 여기에 추가)

## 메모
- 작업/수정/계획을 계속 누적 기록합니다.
